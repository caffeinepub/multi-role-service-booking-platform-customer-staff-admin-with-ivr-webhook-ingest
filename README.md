## Multi-role service booking platform (Customer, Staff, Admin) with IVR webhook ingestion, subscriptions, and payment tracking

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
